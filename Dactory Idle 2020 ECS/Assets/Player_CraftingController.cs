using System.Collections;
using System.Collections.Generic;
using UnityEngine;


/// <summary>
/// This was supposed to handle the crafting, but everything is actually handled by the GUI_CraftingController due to the way code wanted to be written
/// </summary>
public class Player_CraftingController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
